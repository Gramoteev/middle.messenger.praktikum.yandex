.chat-input {
  display: flex;
  width: 100%;
  justify-content: space-between;
  &__text {
    width: 100%;
    border: none;
    border-radius: 10px;
    padding: 10px 20px;
    margin: 0 10px;
    outline: none;
    background: var(--gray-bg);
    resize: none;
  }
}
