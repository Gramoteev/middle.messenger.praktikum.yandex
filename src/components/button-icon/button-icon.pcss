.button {
  &-icon {
    border-radius: 50%;
    background: none;
    border: none;
    &__text {
      font-weight: bold;
      color: var(--gray100);
      display: inline-block;
      padding: 4px;
    }
  }
}
